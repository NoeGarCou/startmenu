# Constants that are not user-editable via the preferences dialog.
# User-facing values (window size, bottom offset, focus behaviour) live in
# settings.py so they can be persisted between sessions.

# D-Bus application ID — must be unique; controls the single-instance mechanism.
APP_ID = "org.nlinux.StartMenu"

# Pixel size for app icons rendered in the grid.
ICON_SIZE_PX = 48
